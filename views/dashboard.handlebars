<div class="mb-5 pt-3">
    <a href="/dashboard/create/">
        <button id="create-post" class="button is-primary is-pulled-right has-text-black">
            Create Post
        </button>
    </a>

    <h4 class="has-text-weight-bold pb-4">
        Dashboard: Your Posts
    </h4>
    <hr>
    {{#if posts.length}}
</div>

{{#each posts as |post|}}
{{> post-info post}}

<a href="/dashboard/edit/{{post.id}}">
    <button class="button mb-5 ml-5 is-warning">
        Edit Post
    </button>
</a>
</div>
{{/each}}

{{else}}
<h6 class="pb-4 has-text-centered">
    You do not have any posts
</h6>

{{/if}}